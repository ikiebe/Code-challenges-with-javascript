const returnVowel = () => {
    let name = "emmanuel";

    for(i=0; i<name.length; i++){
        if(name.includes("e" && "a")){
            console.log(name[i]);
        }
    }
}

returnVowel();